import numpy as np
import 




def Class Mass:

    def __init__(self, mass):
    
        self.mass = mass
        self.a = [0, 1, 2, 3, 4, 5]
        


    def transform(self, axis):

        T = np.zeros((6,6), dtype=np.int8)
        assert type(axis) is 

        if abs(axis) == 1:
        
